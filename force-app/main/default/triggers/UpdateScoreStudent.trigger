trigger UpdateScoreStudent on Score__c (after insert, after update, after delete) {
    // InsertScoreInStudent insertScore = new InsertScoreInStudent();
    // if (Trigger.isInsert || Trigger.isUpdate) 
    // { 
    //     insertScore.updateScoreInStudent(Trigger.new);
    // }
    // else if (Trigger.isDelete)
    // {
    //     insertScore.updateScoreInStudent(Trigger.old);
    // }
}